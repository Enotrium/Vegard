version: '3.8'

services:
  vegard-api:
    build: .
    container_name: vegard-api
    ports:
      - "8000:8000"
      - "50051:50051"
    volumes:
      - ./configs:/app/configs:ro
      - vegard-data:/var/lib/vegard
      - vegard-logs:/var/log/vegard
    environment:
      - VEGARD_CONFIG=/app/configs/default.yaml
      - PYTHONUNBUFFERED=1
    networks:
      - vegard-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  mqtt-broker:
    image: eclipse-mosquitto:2.0
    container_name: vegard-mqtt
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./configs/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - mqtt-data:/mosquitto/data
      - mqtt-logs:/mosquitto/log
    networks:
      - vegard-network
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    container_name: vegard-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./configs/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    networks:
      - vegard-network
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    container_name: vegard-grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./configs/grafana-datasources.yml:/etc/grafana/provisioning/datasources/datasources.yml:ro
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    networks:
      - vegard-network
    restart: unless-stopped
    depends_on:
      - prometheus

networks:
  vegard-network:
    driver: bridge

volumes:
  vegard-data:
  vegard-logs:
  mqtt-data:
  mqtt-logs:
  prometheus-data:
  grafana-data:
